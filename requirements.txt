# deepseek-infra/requirements.txt
torch==2.3.1           # CUDA‑12 wheels available on PyPI
transformers==4.32.0   # 4.29.2 is older than the first 2.x‑ready release
pika==1.3.1
accelerate==0.25.0
hf_transfer>=0.1.0

